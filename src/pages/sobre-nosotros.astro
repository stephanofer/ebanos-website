---
import BaseLayout from "@/layouts/BaseLayout.astro";
import SectionContainer from "@/components/SectionContainer.astro";

const title = "Sobre Nosotros - Mueblería con +30 Años en Chiclayo";
const description = "Conoce la historia de Ebanos Muebles. Desde 1990 fabricando muebles a medida de alta calidad en Chiclayo, Perú. Empresa familiar con experiencia comprobada.";

const breadcrumbs = [
  { name: "Inicio", url: "/" },
  { name: "Sobre Nosotros", url: "/sobre-nosotros" }
];

const fundador = {
  nombre: "Huildor Fernandez Cayao",
  cargo: "Fundador de Ebanos Muebles",
  firma: "/assets/huildorfirma.png",
};
---

<BaseLayout title={title} description={description} pageType="about" breadcrumbs={breadcrumbs}>
  <main class="sobre-nosotros-page">
    <SectionContainer name="sobre-hero">
      <div class="hero-statement reveal">
        <h1>Más de 30 Años de Experiencia</h1>
      </div>
    </SectionContainer>

    <SectionContainer name="sobre-content">
      <div class="content-layout">
        <div class="content-text">
          <div class="text-columns">
            <div class="text-column reveal">
              <p>
                Todo inició en el año 1990, en un viaje para visitar a mi
                hermano Abdías Fernández en Nueva Cajamarca. Él tenía un pequeño
                taller de carpintería y, mirando cómo trabajaba, <strong
                  >me puse a ayudarlo</strong
                >. Al ver que <strong>me desenvolvía muy bien</strong>, me
                propuso trabajar como socios.
              </p>
              <p>
                Estuvimos dos años entregando trabajos y mejorando cada día,
                hasta que nos trasladamos a la ciudad de Chiclayo para conocer
                mejor el mercado en nuestro rubro.
              </p>
              <h3 class="section-subtitle">Creciendo con Esfuerzo</h3>
              <p>
                Para 1995, después de tres años juntos, decidimos separarnos
                como socios para <strong
                  >consolidarnos cada uno con su taller propio</strong
                >. Fue una etapa de mucho trabajo a pedido, donde fui mejorando
                mi maquinaria y asistiendo a capacitaciones constante.
              </p>
            </div>
            <div class="text-column reveal">
              <p>
                Un gran paso fue en 1998, cuando empecé a fabricar para una
                tienda autorizada de <strong>'Singer'</strong>. Durante 12 años,
                fabriqué un promedio de <strong>27,000 muebles</strong> para máquinas
                de coser.
              </p>
              <p>
                Más allá de los números, nuestra mayor satisfacción ha sido
                acompañar a <strong>miles de familias y empresas</strong>,
                fabricando <strong>proyectos únicos</strong> para sus hogares y negocios
                con la misma dedicación del primer día.
              </p>
              <p>
                Hoy, gracias a ese camino, tenemos un taller mejorado, con una
                amplia maquinaria del momento, listos para realizar cualquier
                trabajo con la experiencia adquirida durante el tiempo
                transcurrido.
              </p>
            </div>
          </div>

          <div class="fundador-firma reveal">
            <div class="firma-info">
              <span class="firma-nombre">{fundador.nombre}</span>
              <span class="firma-cargo">{fundador.cargo}</span>
            </div>
            <div class="firma-imagen">
              <img src={fundador.firma} alt={`Firma de ${fundador.nombre}`} />
            </div>
          </div>
        </div>

        <div class="content-image reveal">
          <div class="image-wrapper">
            <img
              src="/assets/sobre-nosotros.webp"
              alt="Taller de Ebanos Muebles - Artesanos trabajando la madera"
              loading="lazy"
              decoding="async"
            />
          </div>
        </div>
      </div>
    </SectionContainer>
  </main>
</BaseLayout>

<style>
  .sobre-nosotros-page {
    padding-top: 2rem;
    padding-bottom: 4rem;
  }

  .hero-statement {
    max-width: 1000px;
    margin-bottom: 4rem;
  }

  .hero-statement h1 {
    font-family: "Clash", sans-serif;
    font-weight: 500;
    font-size: clamp(1.75rem, 4vw, 2.75rem);
    line-height: 1.3;
    color: var(--foreground);
    letter-spacing: -0.02em;
  }

  .content-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .content-text {
    display: flex;
    flex-direction: column;
  }

  .text-columns {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .text-column p {
    font-family: "Inter", sans-serif;
    font-size: var(--fs-base);
    line-height: 1.8;
    color: var(--secondary);
    margin-bottom: 1.25rem;
  }

  .text-column p:last-child {
    margin-bottom: 0;
  }

  .text-column p strong {
    color: var(--foreground);
    font-weight: 600;
  }

  .section-subtitle {
    font-family: "Clash", sans-serif;
    font-size: var(--fs-md);
    font-weight: 500;
    color: var(--foreground);
    margin: 1.5rem 0 1rem;
  }

  .fundador-firma {
    margin-top: auto;
    padding-top: 2rem;
  }

  .firma-info {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    margin-bottom: 1rem;
  }

  .firma-nombre {
    font-family: "Inter", sans-serif;
    font-size: var(--fs-base);
    font-weight: 600;
    color: var(--foreground);
  }

  .firma-cargo {
    font-family: "Inter", sans-serif;
    font-size: var(--fs-sm);
    color: var(--secondary);
  }

  .firma-imagen {
    max-width: 200px;
    opacity: 0.8;
  }

  .firma-imagen img {
    width: 100%;
    height: auto;
  }

  .content-image {
    position: sticky;
    top: 2rem;
  }

  .image-wrapper {
    border-radius: 16px;
    overflow: hidden;
    background: #f5f5f5;
  }

  .image-wrapper img {
    width: 100%;
    height: auto;
    display: block;
    aspect-ratio: 3/4;
    object-fit: cover;
  }

  .valores-section {
    margin-top: 6rem;
    padding-top: 4rem;
    border-top: 1px solid #e5e5e5;
  }

  .valores-section h2 {
    font-family: "Clash", sans-serif;
    font-size: var(--fs-xl);
    font-weight: 500;
    color: var(--foreground);
    margin-bottom: 2.5rem;
    text-align: center;
  }

  .valores-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
  }

  .valor-card {
    text-align: center;
    padding: 2rem 1.5rem;
    background: #fafafa;
    border: 1px solid #e5e5e5;
    border-radius: 16px;
    transition: all 0.3s ease;
  }

  .valor-card:hover {
    border-color: #d0d0d0;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.06);
    transform: translateY(-4px);
  }

  .valor-icon {
    width: 64px;
    height: 64px;
    margin: 0 auto 1.25rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: white;
    border: 1px solid #e5e5e5;
    border-radius: 16px;
    color: var(--foreground);
  }

  .valor-card h3 {
    font-family: "Clash", sans-serif;
    font-size: var(--fs-md);
    font-weight: 500;
    color: var(--foreground);
    margin-bottom: 0.75rem;
  }

  .valor-card p {
    font-family: "Inter", sans-serif;
    font-size: var(--fs-sm);
    line-height: 1.6;
    color: var(--secondary);
    margin: 0;
  }

   .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition:
      opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1),
      transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .reveal.active {
    opacity: 1;
    transform: translateY(0);
  }

  .valor-card.reveal:nth-child(1) {
    transition-delay: 0s;
  }
  .valor-card.reveal:nth-child(2) {
    transition-delay: 0.1s;
  }
  .valor-card.reveal:nth-child(3) {
    transition-delay: 0.2s;
  }
  .valor-card.reveal:nth-child(4) {
    transition-delay: 0.3s;
  }

  .text-column.reveal:nth-child(1) {
    transition-delay: 0.1s;
  }
  .text-column.reveal:nth-child(2) {
    transition-delay: 0.2s;
  }

  @media (prefers-reduced-motion: reduce) {
    .reveal {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }

  @media (max-width: 1200px) {
    .valores-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 1024px) {
    .content-layout {
      gap: 3rem;
    }

    .text-columns {
      grid-template-columns: 1fr;
      gap: 0;
    }
  }

  @media (max-width: 900px) {
    .content-layout {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .content-image {
      position: static;
      order: 1;
    }

    .content-text {
      order: 2;
    }

    .text-columns {
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
    }
  }

  @media (max-width: 768px) {
    .sobre-nosotros-page {
      padding-top: 1rem;
    }

    .hero-statement {
      margin-bottom: 3rem;
    }

    .hero-statement h1 {
      font-size: var(--fs-xl);
    }

    .text-columns {
      grid-template-columns: 1fr;
      gap: 0;
    }

    .valores-section {
      margin-top: 4rem;
      padding-top: 3rem;
    }

    .valores-grid {
      grid-template-columns: 1fr;
      gap: 1.25rem;
    }

    .valor-card {
      padding: 1.5rem;
    }
  }

  @media (max-width: 480px) {
    .hero-statement h1 {
      font-size: var(--fs-lg);
    }

    .fundador-firma {
      text-align: center;
    }

    .firma-imagen {
      margin: 0 auto;
    }
  }
</style>

<script>
  function initScrollReveal() {
    const reveals = document.querySelectorAll(".reveal");

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("active");
          }
        });
      },
      {
        threshold: 0.1,
        rootMargin: "0px 0px -50px 0px",
      }
    );

    reveals.forEach((el) => observer.observe(el));
  }

  document.addEventListener("DOMContentLoaded", initScrollReveal);
  document.addEventListener("astro:page-load", initScrollReveal);
</script>
